import Coordinate from "./coordinate.js";

export default class Plateau{
    constructor(x, y){
        this.corner = new Coordinate(x,y)
        this.start = (0,0)
    }

    isPointValid(x,y){

    }
}